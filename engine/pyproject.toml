[build-system]
requires      = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "engine"
version = "0.1.0"
dependencies = [
    "blessed>=1.33.0",
    "python-dotenv>=1.2.2",
    "PyYAML>=6.0.3",
    "wcwidth>=0.6.0",
]

[tool.hatch.build.targets.wheel]
include = ["engine*"]
